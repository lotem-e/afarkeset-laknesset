import type { Knesset } from "./types";
import raw from "./knessets.json";

// =========================================================
// The single source of truth for the whole site.
//
// knessets.json is GENERATED by scripts/merge-data.mjs from the
// per-era research files ( verified against Hebrew Wikipedia and
// the Knesset site ). To fix a data point: edit knessets.json,
// then run `npm run validate:data` and `npm run validate:colors`
// so the sums and the seat-map colors stay honest.
// =========================================================

export const knessets = raw as Knesset[];
